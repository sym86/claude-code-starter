# Active Memory

> Loaded at every session start. Keep under ~50 lines.
> Move stale entries to `archive.md`.

## Current Focus
- (e.g. "Refactoring the auth module to use JWT refresh tokens")

## Active Decisions
- (e.g. "We're standardizing on pnpm, not npm. Decided 2026-05-10.")

## Open Questions
- (e.g. "Still undecided: should rate limiting live in middleware or gateway?")

## Recent Gotchas
- (e.g. "The test suite needs DATABASE_URL set or it silently skips integration tests.")

## In-Flight Work
- (e.g. "PR #142 — extracting the payment service. Blocked on review.")
